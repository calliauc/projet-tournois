import { BlackWhiteTextDirective } from './black-white-text.directive';

describe('BlackWhiteTextDirective', () => {
  it('should create an instance', () => {
    const directive = new BlackWhiteTextDirective();
    expect(directive).toBeTruthy();
  });
});
